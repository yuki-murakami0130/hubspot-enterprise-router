name: Warm Netlify Function

on:
  workflow_dispatch: {}
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: read

jobs:
  warm:
    runs-on: ubuntu-latest
    steps:
      - name: Curl warm endpoint
        run: |
          curl -sS -m 10 "https://timely-hotteok-d96a87.netlify.app/.netlify/functions/enterprise-check" > /dev/null
